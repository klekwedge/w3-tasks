// Write a JavaScript exercise to create a variable using a user-defined name

interface IObj {
  name: string;
  year: number;
}

const obj: IObj = {
  name: "abcd",
  year: 30,
};

console.log(obj);

// { name: 'abcd', year: 30 }
