"use strict";
// Write a JavaScript program to swap two given variables.
// Swapping two variables refers to mutually exchanging the
// values of the variables. Generally, this is done with
// the data in memory.
let x = 20;
let y = 40;
[x, y] = [y, x];
console.log(x, y);
// 40 20
